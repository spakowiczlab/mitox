---
title: "figure 3 - The association of gut microbes and T-cell senescence markers with immune-related adverse events"
author: "Caroline Wheeler"
date: "2023-10-24"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(ggplot2)
library(ggrepel)
```

# Load data
```{r}
load("../../data/deseq_irae_baseline.rds")
load("../../data/deseq_subset_baseline.rds")
```


Figure 3

Figure 3 - Volcano Plot - irAE yes vs. irAE no
```{r}
g <- ggplot(resPlot_irae, aes(x=log2FoldChange, y=-log10(padj), label = microbe)) + 
  geom_point() + 
  geom_text_repel() +
  theme_minimal()

g
```


Figure 3 - Volcano Plot - Within subset of non-PD irAE yes vs. irAE no
```{r}
g <- ggplot(resPlot_sub, aes(x=log2FoldChange, y=-log10(padj), label = microbe)) + 
  geom_point() + 
  geom_text_repel() +
  theme_minimal()

g
```

